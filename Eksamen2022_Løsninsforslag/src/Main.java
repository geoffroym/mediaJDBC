import java.sql.SQLException;

public class Main {
    public static void main(String[] args) throws SQLException {
    new Program();



        /*TESTS
        QuizService quizService = new QuizService();
        List<Question> mcQuestions = quizService.getQuestions("Football", "Multiple choice quiz");
        mcQuestions.forEach(System.out::println);
        List<Question> bQuestions = quizService.getQuestions("Tennis", "Binary quiz");
        bQuestions.forEach(System.out::println);
        System.out.println("--------------");
        List<QuizTopic> quizTopics = quizService.getAllQuizTopics();
        quizTopics.forEach(System.out::println);
        System.out.println("--------------");
        Optional<User> userCamila = quizService.loginUser("Camila");
        userCamila.ifPresent(System.out::println);
        Optional<User> userPer = quizService.loginUser("Per");
        userPer.ifPresent(System.out::println);*/
    }
}